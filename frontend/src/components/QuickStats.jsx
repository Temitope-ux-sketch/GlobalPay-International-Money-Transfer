import { DollarSign, TrendingUp, AlertCircle } from 'lucide-react';\n\nexport default function QuickStats({ stats }) {\n  if (!stats) return null;\n\n  return (\n    <div className=\"grid grid-cols-1 md:grid-cols-3 gap-6\">\n      <div className=\"bg-white rounded-lg shadow p-6\">\n        <div className=\"flex items-center justify-between\">\n          <div>\n            <p className=\"text-gray-600 text-sm font-medium\">Daily Limit</p>\n            <p className=\"text-3xl font-bold text-dark mt-2\">${stats.dailyLimit}</p>\n            <p className=\"text-xs text-gray-500 mt-1\">Used: ${stats.dailyUsed.toFixed(2)}</p>\n          </div>\n          <DollarSign className=\"text-primary\" size={32} />\n        </div>\n        <div className=\"mt-4 w-full bg-gray-200 rounded-full h-2\">\n          <div\n            className=\"bg-primary h-2 rounded-full\"\n            style={{ width: `${Math.min((stats.dailyUsed / stats.dailyLimit) * 100, 100)}%` }}\n          />\n        </div>\n      </div>\n\n      <div className=\"bg-white rounded-lg shadow p-6\">\n        <div className=\"flex items-center justify-between\">\n          <div>\n            <p className=\"text-gray-600 text-sm font-medium\">Monthly Limit</p>\n            <p className=\"text-3xl font-bold text-dark mt-2\">${stats.monthlyLimit}</p>\n            <p className=\"text-xs text-gray-500 mt-1\">Used: ${stats.monthlyUsed.toFixed(2)}</p>\n          </div>\n          <TrendingUp className=\"text-secondary\" size={32} />\n        </div>\n        <div className=\"mt-4 w-full bg-gray-200 rounded-full h-2\">\n          <div\n            className=\"bg-secondary h-2 rounded-full\"\n            style={{ width: `${Math.min((stats.monthlyUsed / stats.monthlyLimit) * 100, 100)}%` }}\n          />\n        </div>\n      </div>\n\n      <div className=\"bg-white rounded-lg shadow p-6\">\n        <div className=\"flex items-center justify-between\">\n          <div>\n            <p className=\"text-gray-600 text-sm font-medium\">Account Status</p>\n            <p className=\"text-2xl font-bold text-dark mt-2 capitalize\">{stats.accountStatus}</p>\n            <p className=\"text-xs text-gray-500 mt-1\">KYC: {stats.kycStatus}</p>\n          </div>\n          <AlertCircle className={stats.accountStatus === 'active' ? 'text-green-500' : 'text-orange-500'} size={32} />\n        </div>\n      </div>\n    </div>\n  );\n}\n