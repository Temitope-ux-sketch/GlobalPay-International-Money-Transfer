import { useEffect, useState } from 'react';\nimport { apiClient } from '../utils/api';\nimport { Loader } from 'lucide-react';\n\nexport default function RecentTransfers() {\n  const [transfers, setTransfers] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchTransfers = async () => {\n      try {\n        const response = await apiClient.get('/transfers/history');\n        setTransfers(response.data.slice(0, 5));\n      } catch (error) {\n        console.error('Failed to fetch transfers:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    fetchTransfers();\n  }, []);\n\n  return (\n    <div className=\"lg:col-span-2 bg-white rounded-lg shadow-lg p-8\">\n      <h2 className=\"text-2xl font-bold text-dark mb-6\">Recent Transfers</h2>\n\n      {loading ? (\n        <div className=\"flex justify-center py-8\">\n          <Loader className=\"animate-spin text-primary\" size={32} />\n        </div>\n      ) : transfers.length === 0 ? (\n        <div className=\"text-center py-8 text-gray-500\">\n          <p>No transfers yet. Start by sending money!</p>\n        </div>\n      ) : (\n        <div className=\"space-y-3\">\n          {transfers.map((transfer) => (\n            <div key={transfer._id} className=\"flex items-center justify-between p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition\">\n              <div>\n                <p className=\"font-semibold text-dark\">{transfer.recipient.firstName} {transfer.recipient.lastName}</p>\n                <p className=\"text-xs text-gray-500\">Ref: {transfer.referenceId.slice(0, 8)}...</p>\n              </div>\n              <div className=\"text-right\">\n                <p className=\"font-semibold text-dark\">{transfer.amount.sourceAmount.toFixed(2)} {transfer.amount.sourceCurrency}</p>\n                <span className={`text-xs px-2 py-1 rounded-full ${\n                  transfer.status === 'completed' ? 'bg-green-100 text-green-800' :\n                  transfer.status === 'processing' ? 'bg-blue-100 text-blue-800' :\n                  'bg-yellow-100 text-yellow-800'\n                }`}>\n                  {transfer.status.charAt(0).toUpperCase() + transfer.status.slice(1)}\n                </span>\n              </div>\n            </div>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n}\n