import { useAuthStore } from '../store/authStore';\nimport { useEffect, useState } from 'react';\nimport { apiClient } from '../utils/api';\nimport TransferCard from '../components/TransferCard';\nimport QuickStats from '../components/QuickStats';\nimport RecentTransfers from '../components/RecentTransfers';\nimport { Loader } from 'lucide-react';\n\nexport default function Dashboard() {\n  const { user } = useAuthStore();\n  const [stats, setStats] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchStats = async () => {\n      try {\n        const response = await apiClient.get('/users/stats');\n        setStats(response.data);\n      } catch (error) {\n        console.error('Failed to fetch stats:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    fetchStats();\n  }, []);\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-primary/10 to-secondary/10 py-12 px-4 sm:px-6 lg:px-8\">\n      <div className=\"max-w-7xl mx-auto\">\n        <div className=\"mb-8\">\n          <h1 className=\"text-3xl font-bold text-dark\">Welcome back, {user?.firstName}! 👋</h1>\n          <p className=\"text-gray-600 mt-2\">Send money globally in minutes with competitive rates</p>\n        </div>\n\n        {loading ? (\n          <div className=\"flex justify-center py-12\">\n            <Loader className=\"animate-spin text-primary\" size={40} />\n          </div>\n        ) : (\n          <>\n            <QuickStats stats={stats} />\n            <div className=\"grid grid-cols-1 lg:grid-cols-3 gap-6 mt-8\">\n              <TransferCard />\n              <RecentTransfers />\n            </div>\n          </>\n        )}\n      </div>\n    </div>\n  );\n}\n